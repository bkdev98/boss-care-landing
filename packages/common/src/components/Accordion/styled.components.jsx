import styled from 'styled-components';
import { base, theme } from '../../utils';

export const Wrapper = styled.ul`
  padding: 0;
  margin: 0;
  list-style: none;

  ${base}
  ${theme('Acordion')}
`;
