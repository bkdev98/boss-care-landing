import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import Box from '@quocs/common/src/components/Box';
import Switch from '@quocs/common/src/components/Switch';

import Theme from '../../index';

<Meta title="Themes|boss-care/Components/Switch" component={Switch} />

# Switch

Switch is a component to managing boolean state

```js
import Switch from '@quocs/common/src/components/Switch';

<Switch checked={false} onToggle={() => {}} />;
```

<Preview>
  <Story name="Default">
    <Theme>
      <div>
        <Switch checked={false} onToggle={() => {}} />
        <Switch checked={true} onToggle={() => {}} />
      </div>
    </Theme>
  </Story>
</Preview>

## Props

It supports also styled-system props.

<Props of={Switch} />
